<Window x:Class="B1TuneUp.Modules.BarcodeScannerUi.BarcodeScannerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="B1TuneUp - Barcode Scanner"
        Width="460"
        Height="280"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/B1TuneUp;component/Resources/B1Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource PrimaryBrush}" Padding="24" CornerRadius="0,0,24,24">
            <StackPanel>
                <TextBlock Text="B1TuneUp · Barcode Scanner"
                           Foreground="White"
                           FontSize="22"
                           FontWeight="Bold" />
                <TextBlock Text="Transfiere lecturas de código de barras al formulario activo."
                           Foreground="#CCFFFFFF"
                           Margin="0,6,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Margin="24"
                Style="{StaticResource B1CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="Target Item ID" Style="{StaticResource LabelTextBlockStyle}" />
                <TextBox Text="{Binding TargetItemId, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Valor escaneado" Style="{StaticResource LabelTextBlockStyle}" />
                <TextBox Text="{Binding ScannedValue, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="Insertar en SAP"
                        Width="180"
                        Command="{Binding InjectCommand}" />
                <TextBlock Text="{Binding StatusMessage}"
                           Margin="0,12,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
