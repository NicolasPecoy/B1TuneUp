<Window x:Class="B1TuneUp.Modules.ItemActionsUi.ItemActionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="B1TuneUp Â· Item Actions"
        Width="1200"
        Height="720"
        MinWidth="1000"
        MinHeight="620"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="False">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/B1TuneUp;component/B1TuneUp/Resources/B1Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="Button" BasedOn="{StaticResource B1PrimaryButtonStyle}">
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="Margin" Value="0,8,12,0" />
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>

            <Style TargetType="TextBlock" BasedOn="{StaticResource LabelTextBlockStyle}">
                <Setter Property="Margin" Value="0,16,0,6" />
            </Style>

            <Style TargetType="TextBox" BasedOn="{StaticResource B1TextBoxStyle}">
                <Setter Property="AcceptsReturn" Value="False" />
            </Style>

            <Style TargetType="ComboBox" BasedOn="{StaticResource B1ComboBoxStyle}">
                <Setter Property="Padding" Value="6,2" />
            </Style>

            <Style TargetType="Border" x:Key="CardStyle" BasedOn="{StaticResource B1CardBorderStyle}">
                <Setter Property="Padding" Value="20" />
                <Setter Property="Margin" Value="0,0,24,0" />
            </Style>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </ResourceDictionary>
    </Window.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource PrimaryBrush}" Padding="24" CornerRadius="0,0,24,24">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox Width="220"
                             Margin="0"
                             Padding="8,4"
                             Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Buscar por formulario, item o macro"
                             Background="White" />
                    <Button Content="Refrescar" Command="{Binding RefreshCommand}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="B1TuneUp Item Actions"
                               Foreground="White"
                               FontSize="26"
                               FontWeight="Bold"
                               Margin="0" />
                    <TextBlock Text="Administra las macros asociadas a cada Ã­tem de SAP Business One"
                               Foreground="#CCFFFFFF"
                               Margin="0,4,0,0" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardStyle}">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,12">
                        <Button Content="Nuevo" Command="{Binding NewCommand}" />
                        <Button Content="Duplicar" Command="{Binding DuplicateCommand}" />
                        <Button Content="Eliminar" Command="{Binding DeleteCommand}" Background="#FFB33939" />
                    </StackPanel>

                    <DataGrid ItemsSource="{Binding EntriesView}"
                              SelectedItem="{Binding SelectedEntry}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Form Type" Binding="{Binding FormType}" Width="*" />
                            <DataGridTextColumn Header="Item ID" Binding="{Binding ItemId}" Width="*" />
                            <DataGridTextColumn Header="Evento" Binding="{Binding Event}" Width="0.6*" />
                            <DataGridTextColumn Header="Macro" Binding="{Binding Action}" Width="2*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" Grid.Column="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Detalle" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12" />
                        <TextBlock Text="Form Type" />
                        <TextBox Text="{Binding SelectedEntry.FormType, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Text="Item ID" />
                        <TextBox Text="{Binding SelectedEntry.ItemId, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Text="Evento" />
                        <ComboBox ItemsSource="{Binding EventOptions}"
                                  SelectedItem="{Binding SelectedEntry.Event, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Text="Macro" />
                        <TextBox Text="{Binding SelectedEntry.Action, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Height="180"
                                 TextWrapping="Wrap" />

                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <Button Content="Guardar" Command="{Binding SaveCommand}" />
                        </StackPanel>

                        <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,16,0,0" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="24,0,24,12">
            <Border Background="{DynamicResource SurfaceBrush}" Padding="12" CornerRadius="10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding BusyMessage}" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextSecondaryBrush}" />
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="24,0,24,24">
            <Button Content="Cerrar"
                    Width="140"
                    Background="{DynamicResource PrimaryDarkBrush}"
                    Click="OnCloseClick" />
        </StackPanel>

        <Grid Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
              Grid.RowSpan="4"
              Background="#60000000">
            <Viewbox Width="120" Height="120" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Procesando..." Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,8" />
                    <ProgressBar IsIndeterminate="True" Width="120" Height="6" />
                </StackPanel>
            </Viewbox>
        </Grid>
    </Grid>
</Window>


