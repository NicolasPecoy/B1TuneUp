<Window x:Class="B1TuneUp.Modules.QueryExportUi.QueryExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="B1TuneUp - Query Exporter"
        Width="1024"
        Height="680"
        MinWidth="820"
        MinHeight="520"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/B1TuneUp;component/Resources/B1Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource PrimaryBrush}" Padding="24" CornerRadius="0,0,24,24">
            <StackPanel>
                <TextBlock Text="B1TuneUp · Query Exporter"
                           Foreground="White"
                           FontSize="26"
                           FontWeight="Bold" />
                <TextBlock Text="Captura la consulta del formulario activo y expórtala en el formato que necesites."
                           Foreground="#CCFFFFFF"
                           Margin="0,6,0,0" />
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="#CCFFFFFF"
                           Margin="0,6,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Margin="24"
                Style="{StaticResource B1CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <Button Content="Cargar desde formulario"
                            Width="220"
                            Command="{Binding LoadFromActiveFormCommand}" />
                </StackPanel>

                <TextBox Grid.Row="1"
                         Text="{Binding SqlText, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         TextWrapping="Wrap" />

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="Exportar CSV" Width="160" Command="{Binding ExportCsvCommand}" />
                    <Button Content="Exportar JSON" Width="160" Margin="12,0,0,0" Command="{Binding ExportJsonCommand}" />
                    <Button Content="Exportar XML" Width="160" Margin="12,0,0,0" Command="{Binding ExportXmlCommand}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
